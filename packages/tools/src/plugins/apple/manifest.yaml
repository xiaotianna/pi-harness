id: apple
displayName: Apple
apiUrl: http://127.0.0.1:4004
description: Inspect and verify Sign in with Apple flows through the local Apple emulator.
category: productivity
type: oauth
oauth:
  authorizationPath: /auth/authorize
  tokenPath: /auth/token
  scopes: [openid, email, name]
  profile:
    source: id-token
    idField: sub
    usernameField: email
skills:
  - id: apple-sign-in-diagnostics
    displayName: Apple 登录诊断
