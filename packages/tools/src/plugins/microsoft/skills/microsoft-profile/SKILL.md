---
name: microsoft-profile
description: Read the connected Microsoft Graph user profile.
---

# Microsoft 账户

Use `web_fetch` with `{skillGatewayUrl}/api/skill-gateway/microsoft/v1.0/me` to read the connected Microsoft Graph profile. Report only the requested `id`, `displayName`, `mail`, and `userPrincipalName` fields.
