# 前端对话学习规则

每次使用本 Skill 时完整阅读本文件。把这里维护为精简的规则账本，不要写成对话历史或变更日志。

## 记录格式

每条规则必须包含 ID、状态、范围、规则、依据和原因。状态只使用 `active`、`promoted` 或 `superseded`。只转述与规范有关的依据，不保存敏感信息或无关用户内容。

## 规则记录

### WEB-001

- 状态：`active`
- 范围：Provider 图标
- 规则：为 `kimi-coding` 直接渲染 `Kimi` 或 `Kimi.Color` SVG；不要使用 `Kimi.Avatar`，也不要仅为显示图标添加 Avatar 风格 wrapper。
- 依据：用户在修正 Provider 图标对齐时明确拒绝 `Kimi.Avatar`，要求直接使用 Kimi 图标。
- 原因：直接 SVG 与其他 Provider 图标拥有一致的盒模型，并避免隐藏的布局依赖。

### WEB-002

- 状态：`promoted`
- 范围：现有 Web UI
- 规则：修改行为或修复组件使用时，保持当前整体 UI 和组件语言；没有明确要求时，不把局部修正扩大为重设计。
- 依据：用户在要求建立前端规范 Skill 时明确表示当前前端 UI 已基本满意。
- 原因：后续工作应提升一致性，而不是抹掉已经确认的视觉方向。
- 已提升至：`SKILL.md` 的执行流程和样式规范。

### WEB-003

- 状态：`active`
- 范围：Provider OAuth 工作流弹窗
- 规则：保持每个 OAuth 步骤紧凑并本地化 Provider prompt；未开始、初次登录、重新登录和准备中等单一操作或纯状态反馈在弹窗内容区居中，多操作步骤统一放在一个靠右操作行，步骤活动期间不再渲染第二个弹窗级取消操作。状态包含授权地址时，应把用户手势创建的授权窗口自动导航到该地址，并保留显式的“打开授权页面”操作作为弹窗拦截兜底。
- 依据：用户拒绝了直接暴露上游英文、操作分散和重复取消的布局，随后明确要求单一 OAuth 操作及准备中状态反馈居中，并指出手动授权码步骤缺少前往授权页面的路径。
- 原因：居中的单一操作能形成明确焦点，统一操作行避免层级混乱，而自动导航配合显式入口可确保授权状态始终可操作。

### WEB-004

- 状态：`promoted`
- 范围：Skill 职责
- 规则：本 Skill 必须覆盖完整前端工程规范，组件使用规范只是其中一部分；正文和规则资料使用中文。
- 依据：用户明确要求把 Skill 改为中文，并把范围从组件约束扩大到完整前端规范。
- 原因：Skill 名称、触发描述和内容范围应准确反映项目实际使用方式。
- 已提升至：`SKILL.md`、`project-map.md` 和 Skill 元数据。

### WEB-005

- 状态：`promoted`
- 范围：代码与协议复用
- 规则：新增常量、枚举、类型、协议或工具函数前先全仓搜索并复用已有实现；受模块边界限制时，由原所有者提供最小安全导出，不在消费者中复制。API 边界必需的运行时校验 Schema 除外。
- 依据：用户指出 Web 重复定义了 runtime 已有的 `HarnessEventType`，并明确要求将“已有代码或枚举不要重写”加入项目规范。
- 原因：单一来源可以避免 runtime、daemon 与 Web 之间发生协议漂移。
- 已提升至：仓库 `AGENTS.md`、`SKILL.md` 和 `project-map.md`。

### WEB-006

- 状态：`superseded`
- 范围：对话输入与模型切换
- 规则：Agent 运行期间消息输入框保持可编辑，“+”菜单保持可打开；尚未接入的能力只禁用对应菜单项，不禁用整级入口。模型切换保持禁用直到 run 进入终态，并显示禁用鼠标样式。
- 依据：用户明确要求运行期间仍能输入，同时要求模型切换等待停止后恢复并提供禁用鼠标反馈；随后明确指出“+”入口不应被禁用。
- 原因：用户可以提前准备下一条消息，同时避免活动 run 中途改变模型配置。
- 替代规则：`WEB-027`

### WEB-007

- 状态：`superseded`
- 范围：消息操作与失败状态
- 规则：用户消息在气泡尾部稳定显示复制操作；助手消息操作使用复制、重新生成、点赞和点踩，不使用无明确操作的更多菜单；run 失败使用语义化错误提示呈现。
- 依据：用户根据真实对话界面明确指出用户消息缺少复制、助手消息更多菜单应替换为点赞和点踩、裸错误文本观感不佳，并在后续检查中要求恢复未显示的消息按钮。
- 原因：常用操作应直接可见，失败状态需要明确层级和可访问语义。
- 替代规则：`WEB-008`

### WEB-008

- 状态：`active`
- 范围：Markdown 消息、消息操作与失败状态
- 规则：消息操作作为气泡下方独立操作行，不覆盖正文；有 hover 能力时默认隐藏，用户消息操作在悬停对应用户消息时显示，AI 尾部操作在悬停所属 turn 的任意消息时显示；键盘聚焦操作时显示，触屏设备保持可见。消息完成时间复用操作行的相同显隐规则，使用本地日历日判断：今天只显示 `HH:mm`，昨天显示“昨天 + 时间”，更早显示日期与时间，跨年时补充年份；保持常规字重和区别于操作图标的更弱颜色。用户消息时间位于操作图标左侧；AI 只在 run 完成、失败或中止后，于该 turn 的最末尾展示一次操作和结束时间，运行中不展示。同一 run 内不跟随每个 `message.completed` 或内部模型 turn 渲染操作；没有可复制的助手文本时只展示结束时间。最后一条助手消息本身已处于 turn 末尾时，操作必须继续留在原 `ChatMessage.Body` 内，完整复用原有间距；只有工具或错误等内容位于正文之后时，才把操作作为独立尾部行。助手 Markdown 消息只组合 `ChatMessage.Assistant`、`ChatMessage.Body`、`ChatMessage.Content` 和 `Markdown`，不渲染头像或媒体。所有助手消息的 `ChatMessage.Body` 不保留 Pro 默认的尾部内边距，让正文、变更摘要和操作区使用完整可用宽度。run 失败使用 assistant-ui `ErrorState` 的结构与 HeroUI danger 语义色呈现，占满消息内容区，只显示标题和错误详情。
- 依据：用户明确要求 Markdown 消息使用 HeroUI Pro `ChatMessage` 的核心消息部分，并连续纠正了消息操作的位置、显示时机，以及自动续轮时操作只能出现在最终 AI turn 结束处；随后指定 run 失败状态使用 assistant-ui `ErrorState`，保留 HeroUI 色彩和精简内容，并要求宽度占满消息内容区；之后通过截图明确要求移除助手消息 Body 的尾部内边距；用户进一步要求消息时间按本地日期明确区分，并要求 AI 的操作与时间在 run 完成、失败或中止后显示于 turn 最末尾，运行中不显示；尾部操作独立后，又明确纠正其 hover 范围必须覆盖所属 turn，而不是只覆盖底部操作行自身，并要求正文与操作之间的既有样式和距离保持不变。
- 原因：Turn 级 hover 让放在末尾的 AI 操作仍与整段回复保持同一交互范围，避免用户必须精确移动到底部才能发现操作；以 run 终态识别用户可见的 AI turn，可以避免把内部续轮的单条消息完成时间误当成最终回复，同时保留键盘与触屏可用性；移除共享 Body 的尾部预留可让所有助手内容使用一致的完整宽度，独立错误展示原语则保持失败反馈一致并充分利用消息空间。

### WEB-009

- 状态：`active`
- 范围：侧栏 Session 操作
- 规则：侧栏 Session 条目保留尾部 `…` 更多操作入口，并提供真实可用的重命名与归档操作；归档只隐藏侧栏条目并保留 Session 索引与 JSONL，在设置的“已归档”中按 Workspace 分组记录，并允许恢复；Workspace 分组标题固定单行，超出可用宽度时省略。不要与消息操作栏的更多按钮混淆。
- 依据：用户明确澄清缺失的是侧栏对话条目的 `…` 菜单，其中需要重命名和归档；后续进一步要求归档不能删除，需要在设置中按 Workspace 记录，并通过截图明确归档列表中的 Workspace 标题只展示一行。
- 原因：Session 管理属于侧栏条目上下文，操作入口和持久化能力必须同时存在；可恢复的设置记录让归档与删除保持清晰区别。

### WEB-010

- 状态：`promoted`
- 范围：辅助函数与 Agent Prompt 归属
- 规则：与业务无关的通用辅助能力使用项目已有第三方库并放入所属模块 `utils`；业务相关的判断、解析和转换辅助函数同样归入所属模块 `utils`；模型和 Agent Prompt、Prompt 片段及协议标记统一放入 `prompts`。
- 依据：用户明确要求该约束同时适用于后端、前端和 packages，并指出 Runtime 中对象判断、内部消息判断和续轮 Prompt 的职责归属错误。
- 原因：统一归属可以避免业务文件重复实现基础能力，也能防止 Prompt、协议和编排职责再次混杂。
- 已提升至：仓库 `AGENTS.md` 和本 Skill 的文件与导出规范。

### WEB-011

- 状态：`active`
- 范围：侧栏工作区折叠状态
- 规则：持久化用户对各工作区的展开或折叠选择，桌面与移动侧栏共享状态；未保存偏好的新工作区默认展开。空工作区仍允许展开，并显示明确的空状态反馈。
- 依据：用户明确指出工作区折叠状态不能在每次初始化时重置为关闭。
- 原因：折叠属于跨初始化保留的纯 UI 偏好；保存折叠项可同时保留用户选择，并让异步加载或新增工作区沿用默认展开行为。

### WEB-012

- 状态：`active`
- 范围：本地工作区添加与会话绑定
- 规则：添加工作区时由 daemon 打开操作系统目录选择器并持久化校验后的真实路径；Web 使用工作区 ID 创建会话，不把浏览器文件选择结果或客户端路径作为工作区事实来源。侧栏与新对话工作区菜单复用同一添加流程；点击某个工作区行尾的“+”时，先把该工作区设为新对话的项目选择，再进入新对话页。工作区选择菜单中的名称固定为单行，超出可用宽度时省略。侧栏工作区条目为操作区预留空间并提供 `…` 菜单，菜单依次包含编辑、按操作系统命名的目录显示操作和移除项目；移除直接执行，不展示破坏性确认，只隐藏引用并保留本地文件与会话，再次添加同一目录时恢复原会话。
- 依据：用户明确要求选择本地目录，并让后端记录真实目录位置供后续工作区内容操作使用；随后明确指出软移除不是破坏性操作，不需要二次确认，并要求侧栏工作区“+”同步新对话的项目选择。
- 原因：浏览器目录选择无法提供可信绝对路径；由本地 daemon 持有真实路径，才能让后续文件与 Shell 能力安全绑定不可变工作区边界。工作区内的新对话入口必须保留其目录上下文；软移除可恢复且不删除数据，额外确认只会增加操作阻力。

### WEB-013

- 状态：`active`
- 范围：Tooltip 交互
- 规则：Tooltip 移出触发器后立即开始关闭，不保留额外关闭延迟；通过 HeroUI 全局主题变量统一控制，不在各调用点重复配置。
- 依据：用户明确指出 Tooltip 消失过慢会影响连续操作。
- 原因：操作型 Tooltip 在指针离开后继续停留会遮挡界面，并干扰相邻控件的快速操作。

### WEB-014

- 状态：`active`
- 范围：工作区排序
- 规则：工作区目录允许在侧栏拖拽调整顺序，并提供键盘等价操作；拖拽浮层保持为只含文件夹图标和名称的紧凑圆角条目，插入位置使用带起点圆点的强调色横线。排序由 daemon 持久化到本地 SQLite，Web 只更新 Query 缓存，不写入 `localStorage`。
- 依据：用户明确要求目录支持拖拽排序，并确认顺序应保存在本地文件而不是浏览器存储；随后指出浏览器默认拖拽快照不符合项目风格，并指定参考紧凑浮层与横线指示器。
- 原因：工作区顺序属于跨浏览器初始化保留的本地应用数据，应与工作区引用使用同一事实来源；精简的浮层和明确插入线能保留上下文，同时避免复制整行操作区造成视觉噪声。

### WEB-015

- 状态：`active`
- 范围：侧栏工作区状态
- 规则：当前上下文所属工作区的文件夹图标使用强调色，收起和展开图标都保留同色系的浅色内部填充；其他工作区图标使用 muted 灰色。已存在的会话按 Session 的工作区判断；尚未创建 Session 的新对话按输入器当前选择的工作区判断。展开状态只切换文件夹开合图标，不改变状态颜色。Gravity UI 的文件夹轮廓与对应 Fill 图标成对叠加，不能因其内部 path 固定 `fill` 而丢失软色填充。
- 依据：用户明确要求当前目录下的对话对应蓝色文件夹图标，其他目录保持灰色；随后指出新对话即使尚无 Session，也已经属于输入器所选工作区，应显示相同的蓝色状态；替换为 Gravity UI 图标后又明确要求收起和展开目录都恢复浅蓝内部填充。
- 原因：颜色应表达当前上下文的目录归属，是否已经产生消息或 Session 不影响归属；文件夹开合仅表达结构展开状态，二者不能混用；分别叠加 Fill 与轮廓图标可保留 Gravity UI 几何，同时恢复原有的软色层级。

### WEB-016

- 状态：`superseded`
- 范围：助手 Markdown 流式渲染
- 规则：助手 Markdown 使用 Streamdown，并将活动回复的流式状态传给 `isAnimating`；启用内容动画和块状 caret，静态消息关闭动画状态。
- 依据：用户明确指出当前 Markdown 不是流式渲染，并指定用 Streamdown 的动画与 caret 能力替换现有 Markdown 组件。
- 原因：Streamdown 能在增量内容尚不完整时稳定解析 Markdown，并只为当前活动回复呈现流式反馈。
- 替代规则：`WEB-017`

### WEB-017

- 状态：`active`
- 范围：助手 Markdown 与会话滚动
- 规则：助手回复使用 HeroUI Pro `Markdown` 的 memoized block 流式渲染，并通过统一的 `components` 映射配置 Markdown UI；真实会话的助手正文以 daemon 事件内容为唯一目标值，流式提交方式遵循 `WEB-033`。相邻 memoized block 必须保留标题的标准顶部间距，以及列表与相邻正文的标准块间距，不能因标题、列表或段落被拆成独立 block 而贴在一起。绝对 `http://` 和 `https://` 链接使用 HeroUI `Link`，在文本后追加内置 `Link.Icon`，保持默认无下划线、hover 时显示下划线，并在新页面打开；页面锚点和其他协议保留原生 Markdown 链接。本地文件链接由独立的 `AssistantMarkdownLink` 处理，支持绝对路径、相对路径及行号后缀：使用接近正文高度、与文案垂直居中的主题蓝色文件图标，并让整个 `inline-flex` 链接盒子与相邻正文行中线对齐；默认无下划线，hover 或键盘聚焦时显示虚线下划线，Tooltip 展示按当前 Workspace 补全的完整路径；链接显示内容取自 Markdown 的 `[]` 文案，与 `()` 中的真实路径分离，并允许长文案在可用宽度内换行。消息列表拦截点击并向 daemon 发送 `workspaceId` 和原始路径；daemon 必须复用路径策略解析真实路径、拒绝 Workspace 外路径和受保护目录，再用系统默认应用打开。GFM 表格使用 HeroUI `Table`，任务列表勾选框使用只读 HeroUI `Checkbox`；`chart` 使用图表组件，`formula` / `math` / `latex` 使用公式组件，`flow` 使用 JSON 步骤数据和 HeroUI `Surface` 卡片，步骤保持可读宽度并在超出消息区时横向滚动；`flowchart` / `mermaid` 使用 Mermaid DSL；Mermaid 的测量节点必须渲染在组件内部受 overflow 约束的容器中，流式源码变化需要先合并，停止变化后再绘制，不能在每次 token 更新时反复切换 loading 与图。结构化块解析或 Mermaid 渲染失败时回退为普通 `CodeBlock`。回复正文和 Thinking 共用该映射。会话内容增长时，距离底部不超过 96px 自动跟随；超过阈值后保持用户阅读位置，并保留显式滚动到底部操作。
- 依据：用户基于 HeroUI Pro Markdown 文档确认恢复项目原组件，并明确要求 AI 输出根据底部距离阈值决定是否跟随滚动；随后要求表格和任务勾选框改用 HeroUI，并像 MDX 一样集中配置每类 Markdown 块的 UI 组件，又补充需要图表、公式和流程图 mock 及对应渲染组件，最终确认 `flow` 恢复 HeroUI 卡片，`flowchart` 与 `mermaid` 使用 Mermaid DSL；之后指出任务列表与后续标题之间缺少间距，本次又通过截图明确列表与前后正文都需要块间距；并进一步确认 Mermaid 抖动来自流式源码变化时 loading 与图的反复切换；随后指定 HTTP(S) Markdown 链接使用 HeroUI Link 和内置外链箭头，并进一步纠正应保留官网默认的 hover 下划线交互且点击后打开新页面；后续要求本地路径点击后发送请求并由系统默认应用打开，并明确补充相对路径解析、文件图标、虚线 hover 下划线和完整路径 Tooltip；之后进一步明确链接文案不等于完整路径且长链接内容需要正常换行，本次补充文件图标应放大并与文案垂直居中；本次又明确要求大 chunk 也必须以打字机方式逐步显示，同时保证前后端事件内容一致；后续截图进一步指出本地文件链接整体没有与同一行正文对齐；本次截图明确 `flow` 横向步骤应滚动，不能平均挤压成逐字换行。
- 原因：按块更新可保留项目既有 Markdown 视觉，但独立 block 会让标题错误命中首元素零上边距，需要在 block 边界恢复垂直节奏；Markdown block 的内容哈希 key 会让流式变化中的 Mermaid 组件反复挂载，立即绘制会在 loading 和不同高度的图之间切换，需要延后到源码稳定后再渲染；组件内测量容器同时避免临时 SVG 影响页面布局。`flow` 步骤固定在可读宽度后由原生 overflow 承担横向浏览，避免步骤数量改变节点文字排版。HeroUI Pro Markdown 的通用 `a` 样式会覆盖 HeroUI Link 的默认下划线状态，需要只对 `.markdown .link` 恢复组件交互。链接判断独立后，Markdown 主映射保持精简，相对路径展示可复用当前 Workspace 上下文；本地文件仍必须交给 daemon，并在执行系统命令前保持 Workspace 和受保护路径边界。`align-items` 只能对齐链接内部的图标和文案，行内 `inline-flex` 还需要显式 `vertical-align` 才能与相邻正文对齐。集中映射能让所有 AI Markdown 消费者使用一致的 UI 组件；对未完成或非法结构回退为代码可以保留模型原始输出；滚动阈值兼顾持续查看最新输出与向上阅读历史消息。
- 嵌套列表间距更新：带子列表的同级分组使用与父项到子列表一致的 12px 间距，普通扁平列表仍保持紧凑间距。
- 嵌套列表间距依据：用户通过真实会话截图明确指出父项上下两部分的间距不一致。
- 本地文件打开边界更新：由 `WEB-031` 替代本条中的 Workspace 外路径限制。

### WEB-018

- 状态：`superseded`
- 范围：加载状态语义
- 规则：AI 等待与生成状态统一使用 AICSS `Orbs` S2；骨架屏只用于页面数据请求期间，不用于模型生成、工具执行或其他 AI 活动状态。
- 依据：用户明确将 Orbs S2 声明为统一规范，并要求页面数据加载使用骨架屏。
- 原因：区分 AI 活动与数据请求，避免同一种加载反馈表达不同语义。
- 替代规则：`WEB-022`

### WEB-019

- 状态：`active`
- 范围：会话消息渲染模块
- 规则：`ThreadMessage` 保持稳定目录入口；消息渲染策略组合和各独立消息组件分别放入 `components/thread-message/` 内的独立文件。
- 依据：用户明确要求拆分原有单文件中的 `MESSAGE_RENDER_STRATEGIES` 组合与每个独立组件。
- 原因：消息类型持续增加时，独立模块可以缩小单文件职责和改动范围。

### WEB-020

- 状态：`active`
- 范围：工具审批交互
- 规则：待处理的工具审批固定显示在会话底部并覆盖消息输入器；消息时间线中的 Tool 只呈现等待审批等工具状态，不在消息内重复展示审批详情或确认操作。
- 依据：用户明确要求参照 Codex 将审批放到底部输入区，并指出现有消息内审批卡片只应保留工具状态。
- 原因：审批是当前必须处理的会话级交互，覆盖输入器能形成唯一操作焦点，同时避免消息流与底部区域重复提供确认入口。

### WEB-021

- 状态：`active`
- 范围：列表项异步开关
- 规则：列表项共用 mutation 时，开关的 pending、disabled 等可见状态必须按当前操作项 ID 限定；受控开关应立即乐观更新 Query 缓存，用服务端响应校准，失败时只回滚当前项，不通过整列表失效刷新完成切换。
- 依据：用户指出切换某个 Provider 的 Switch 时其他 item 也会刷新，并在限定 disabled 状态后进一步反馈开关仍会闪烁。
- 原因：受控开关若等待请求完成后重拉列表，会经历旧值回弹和新值再次切换；局部乐观更新可以保持即时反馈并避免无关控件闪动。

### WEB-022

- 状态：`active`
- 范围：AI 状态与过程展示
- 规则：AI 等待、思考、推理、工具调用、图片生成和任务列表使用直接放在 `components/ai` 下的 assistant-ui Elements；初始等待与生成状态只显示 Shimmer 状态文案，不显示九宫格 dot。Web Search 只使用 HeroUI Pro `ChatSource` 的 Stacked Favicons 与 `ChatSources` Grouped 展示，不叠加旧查询胶囊。每个组件独立成文件。`ReasoningPanel` 和 `ToolCall` 分别以 assistant-ui 官方 Element 的 API 与结构为准，基础折叠原语适配为 HeroUI `Disclosure`。Think 使用 100% 消息宽度；标题使用 `Thinking...` 加行内代码样式的当前状态，活动态只让 `Thinking...` 使用 Shimmer，状态文本保持静态；正文直接使用 Markdown 渲染，不增加圆点或“分析”等步骤标签，达到最大高度后在组件内部滚动，正文使用 muted 前景色且与标题左右边缘对齐。Think 流式输出期间保持展开，当前 Think 输出结束后立即自动收起，之后仅由用户手动重新展开。`ToolCall` 状态图标紧跟工具名，详情默认收起，整体限制为 `max-w-sm`。同一 LLM 消息返回两个及以上 Tool Call 时，外层只显示调用进度并折叠各工具详情；单个 Tool Call 直接展示。
- 依据：用户明确要求前端 AI 组件独立放在 `components/ai` 下，并分别指定 assistant-ui 的 Reasoning panel、Tool call，以及 HeroUI Pro Chat Source 的 Stacked Favicons + Grouped 用于 Web Search；随后连续确认 ToolCall 的宽度、折叠和状态图标位置，并要求 Think 使用 Markdown、多工具调用使用外层进度折叠，进一步指定 Think 的 Shimmer 标题、状态样式、正文结构与内部滚动方式；之后又明确要求 Think 标题与正文对齐、正文弱化颜色，并在流式输出结束时自动收起；本次要求移除初始思考状态上方的 dot 动画。
- 原因：保持 AI 展示组件职责清晰，让 Web Search 的触发与来源内容完全由指定的分组组件表达；按 LLM 单次返回的工具数量决定展示层级，可以保留执行进度并减少重复视觉噪声。Think 使用次级文字颜色和一致边缘可以降低过程内容的视觉权重，按流式状态自动折叠则避免已完成过程持续挤占会话空间。

### WEB-023

- 状态：`superseded`
- 范围：探索页 Agent 演示
- 规则：探索页的完整回复演示复用正式 HarnessEvent 消息链路与渲染组件。Turn 运行中按 LLM 返回顺序流式展示每轮 thinking、text 和 Tool Call；Turn 进入终态后，把最后一条助手 text 之前的中间内容整体默认收起，只保持最终回复可见。终态聚合折叠条显示“已处理”、实际执行时长和展开箭头，摘要行下方使用贯穿可用宽度的半像素细分割线，并收紧分割线与最终回复的间距；不显示完成状态图标。该样式只用于整个 Turn 全部完成后的聚合区域。mock 覆盖当前工作区工具能力，最终回复展示常用 GFM、图表、公式、当前 Workspace 下的多个可打开绝对文件路径；本地文件链接使用简短普通文案展示，真实路径只放在 href 和 Tooltip 中。`# flow`、`# flowchart` 标题分别展示 HeroUI Flow 和 Mermaid 流程图。
- 依据：用户先要求探索页展示类似真实 AI 回复的多轮思考与工具调用，随后明确补充中间内容包含 thinking 与 text，运行时依次流式展示，Turn 完成后整体折叠并保留最后消息；之后指定终态聚合折叠条参考“已处理 + 执行时间 + 分割线”的样式，且不影响内部单项折叠，并进一步纠正分割线应位于摘要行下方而非箭头右侧；后续又指出分割线需要更细、与最终回复的间距应更紧凑，并要求补充图表、公式和流程图 mock，最终确认 `flow` 与 `flowchart` 分别展示 HeroUI 卡片和 Mermaid 流程图；之后要求增加本地绝对路径 mock，并验证点击后可由系统默认应用打开；本次明确 mock 的链接显示文案应与真实路径分离并使用普通文本。
- 原因：复用正式事件顺序和终态语义，可以让探索演示与真实 Agent Turn 保持一致，并避免过程内容长期挤占完成后的会话空间；执行时长为聚合摘要提供有用信息，分割线则清晰区分过程与最终答复；可点击绝对路径让探索页覆盖本地文件交互的完整链路。
- 替代规则：`WEB-072`

### WEB-024

- 状态：`active`
- 范围：Session 与侧栏滚动容器
- 规则：真实会话、侧栏目录/会话列表及其内层滚动区域统一使用透明轨道、浅色细圆角滑块的悬浮滚动条样式，滚动条颜色以低透明度 `muted` token 适配明暗主题，并使用稳定 scrollbar gutter，避免滚动条出现或消失引起内容宽度变化和页面跳动。侧栏固定区域包含“新会话”、分割线、“工作区”标题和添加按钮，只允许其下方具体工作区目录及会话树滚动。
- 依据：用户明确要求 Session 滚动条统一改为悬浮样式以避免页面跳动，随后指出侧栏滚动条不可见，并要求“新会话”及其分割线不跟随列表滚动；之后以 Codex 滚动条为参照，指出当前滑块颜色过深、不符合项目风格，又进一步明确“工作区”标题和添加按钮也应固定，只有具体目录可以滚动。
- 原因：浅色、低干扰的滚动条能保持可发现性而不抢占内容视觉层级，稳定 gutter 可保持内容区宽度不随溢出状态变化；固定主要操作和分组标题可让长目录列表中仍能随时识别当前列表并创建工作区或新会话。

### WEB-025

- 状态：`promoted`
- 范围：权限审批设置与消息输入器
- 规则：权限审批策略同时显示在通用设置和消息输入器工具栏，两处保持选项 label 一致，并通过同一 TanStack Query 读写 daemon `app_settings` 中的全局值，所有 Session 共用。设置页沿用简洁文案，使用与相邻设置项一致的无图标、无额外强调色 HeroUI secondary Select 和等宽尺寸；消息输入器使用带图标与说明的 HeroUI Dropdown，说明固定在 label 下方另起一行，完全访问项使用 danger 与 warning 语义混合的红橙色。输入器触发器选中请求批准或帮我批准时，图标、文字和箭头统一使用 muted 灰色；选中完全访问时三者统一使用红橙色；三个权限策略图标分别使用 Lucide `Hand`、`ShieldCheck` 和 `ShieldAlert`，下拉箭头使用 Gravity UI，并与相邻工作区选择器保持一致。切换到完全访问时，必须先用 HeroUI AlertDialog 说明不再逐次询问及仍然生效的安全边界；取消保持原策略，明确确认后才进入持久化流程。
- 依据：用户明确要求参照权限审批菜单，将同一下拉选项同时放入设置和 prompt 输入框，并要求内容保存在本地文件；随后确认两处配置共享同一全局审批策略，并要求完全访问需弹窗确认；本次进一步明确三个权限策略图标都使用 Lucide。
- 原因：单一服务端事实来源能让所有入口和 Session 保持一致，并确保 Agent 执行策略使用已持久化的当前配置。
- 已提升至：`project-map.md` 与 `docs/架构设计.md`。

### WEB-026

- 状态：`active`
- 范围：异步紧凑选择器
- 规则：权限、模型等由查询数据驱动的紧凑选择器，在首次查询尚未返回真实值或选项时，于触发器位置显示尺寸匹配的 HeroUI Skeleton；取得真实数据后再渲染 Select 或 Dropdown。保存已有值时保留当前选择并禁用控件。
- 依据：用户明确要求权限和模型配置在加载完成前使用骨架屏，不展示推定初值或加载文案。
- 原因：骨架屏能避免错误选择短暂闪现，并保持工具栏与设置行的布局稳定；保存阶段保留真实值则避免控件回退为加载状态。

### WEB-027

- 状态：`active`
- 范围：运行态消息输入与模型切换
- 规则：Agent 运行期间输入器保持正常视觉和完整编辑能力；有内容时允许发送并排入当前 Run 的后续队列，内容为空时发送按钮仍用于停止 Run。“+”菜单、权限策略和模型选择保持可操作。已有 Session 中切换模型保存为该 Session 的后续 Run 配置，不改变正在执行的 Run，也不修改全局默认模型；新会话页与设置页通过同一 TanStack Query 读写 daemon `app_settings` 中的全局默认 Provider、模型和推理强度，刷新浏览器后必须恢复，之后创建的 Session 复制该值，不在 Zustand 或 `localStorage` 维护副本。
- 依据：用户明确要求运行中输入框不变灰，允许编辑、发送后续操作和切换模型，不对这些输入器能力增加运行态限制；随后明确已有会话保持自己的模型，新会话页的模型选择就是设置中的全局默认模型；本次进一步指出输入器和设置页的默认模型刷新浏览器后丢失，要求保存到本地 daemon 配置。
- 原因：运行态输入用于提前安排后续工作；复用 PromptInput 的运行中提交语义可以同时保留发送和停止操作。区分 Session 模型与新会话全局默认值，可以避免全局切换追溯修改已有会话，同时让两个默认模型入口保持同一状态来源。

### WEB-028

- 状态：`active`
- 范围：助手 Markdown 排版
- 规则：助手 Markdown 中原本使用 14px 小字号的正文、三级标题和表格统一使用 15px。
- 依据：用户明确指出 Markdown 正文相关内容的 14px 字号偏小，要求统一调整为 15px。
- 原因：统一提高 Markdown 小字号可改善长回复的阅读舒适度，并保持各类正文内容字号一致。

### WEB-029

- 状态：`superseded`
- 范围：真实会话中间过程流式展示
- 规则：运行中的 Thinking 和中间助手正文都必须按 daemon 事件顺序出现，并对大 chunk 按动画帧逐步揭示；揭示速度根据待显示字符积压量动态追赶，慢模型保持自然增量，快模型提高每帧字符数。普通的 `message.completed`、工具开始及工具结果必须等前面的可见内容完全追上后再展示；收到已经开始服务端倒计时的 `approval.requested` 时，必须立即补齐它之前的可见内容并展示审批，不得让展示动画消耗用户的审批时间。Thinking 等可见内容追上后再自动收起，最终可见内容必须与对应完成事件一致。
- 依据：用户先指出工具调用之间的 Thinking 和中间正文仍然一次性出现，随后进一步指出前文尚未显示完时工具已经出现或完成，并要求根据模型 chunk 动态调整输出速度；在按序缓冲后，又指出长内容可能让已经开始计时的工具审批在展示前超时。
- 原因：过程消息通常紧接工具调用并快速完成，只给文字组件添加独立打字动画会让后续事件越过仍在追赶的正文；统一的展示队列才能保持普通事件顺序，但带服务端期限的人机交互必须作为紧急边界，避免表现层延迟侵占真实响应窗口。
- 替代规则：`WEB-033`

### WEB-030

- 状态：`superseded`
- 范围：AI 消息中的 URL 与本地文件链接
- 规则：Agent Prompt 要求 HTTP(S) URL 和本地文件引用使用带描述文案的 Markdown Link，不使用行内代码；Web 仅对行内代码中完整的 HTTP(S) URL 和带文件名扩展名的绝对路径自动链接兜底，其他代码片段保持代码语义。本地路径点击继续经过 daemon 的 Workspace 与受保护路径校验。
- 依据：用户指出同样的 URL 和文件路径在 Codex 中可点击，但当前会话因模型使用反引号而只显示为代码片段。
- 原因：Prompt 负责生成正确语义，窄范围渲染兜底可以兼容已有或偶发的不规范输出，同时避免把 API 路径、命令和普通标识符误判成链接。
- 替代规则：`WEB-031`

### WEB-031

- 状态：`promoted`
- 范围：AI 消息中的本地路径打开边界
- 规则：用户主动点击本地路径链接时，相对路径按当前 Workspace 解析，绝对路径可位于任意位置；href 必须使用真实且可独立解析的路径，不得加入示例占位前缀。只要 `realpath` 后路径真实存在，就交给操作系统打开，普通文件使用默认应用，目录使用文件管理器，不额外限制 Workspace、文件类型或受保护目录。该规则不适用于 Agent 文件工具或 Shell，它们仍须维持 Workspace 与受保护路径边界。
- 依据：用户先明确要求消息中的文件只要真实存在就应能够定位，即使文件位于当前 Workspace 外；后续点击真实存在的目录被“不是普通文件或受保护目录”拦截时，进一步明确用户主动查看本地路径不应套用 Agent 文件读取限制；本次又指出带本地链接语义的地址不应因提示词示例前缀而无法打开。
- 原因：用户主动把已展示路径交给本机操作系统打开，不会把路径内容读取到 Web 或模型，与 Agent 自主访问文件属于不同权限边界；这里只需要确认路径存在，Agent 工具仍由原安全策略约束。使用真实 href 还能避免展示层级被错误拼入文件系统路径。
- 已提升至：`project-map.md` 与 `docs/架构设计.md`。

### WEB-032

- 状态：`active`
- 范围：消息输入器工具栏
- 规则：工作区选择器与相邻权限选择器的名称、图标和箭头统一继承组件默认字重与描边宽度。
- 依据：用户明确要求相邻工作区与权限选择器的字体和图标以工作区选择器为基准保持一致。
- 原因：相邻的同级紧凑选择器应保持一致的文字与图标视觉重量。

### WEB-033

- 状态：`active`
- 范围：真实会话流式内容展示
- 规则：Web 收到 SSE `message.delta` 后必须立即按事件顺序拼接完整 chunk，不拆分字符、不延迟事件，也不让表现层动画阻塞 `message.completed`、工具或审批事件。打字感只使用不改变内容进度的轻量视觉动画，并尊重减少动效偏好；流式 Markdown 正文末尾不显示块状 caret；Thinking 在对应流结束后自动收起。
- 依据：用户明确纠正大 chunk 的逐帧打字机实现会导致前后端不同步，要求每个 SSE chunk 到达后立刻拼接，只保留过渡动画；本次截图进一步要求移除流式消息末尾的黑色块状 caret。
- 原因：SSE chunk 已经表达服务端真实增量节奏；表现层再次控制字符进度会制造积压，使消息和后续事件晚于 daemon 状态。

### WEB-034

- 状态：`active`
- 范围：会话工具与文件变更展示
- 规则：文件读取和写入类工具的过程行显示具体文件名，命令工具显示实际执行命令；工具状态文案固定单行，文件名或命令摘要按内容宽度展示并设置最大宽度，不得拉伸填满剩余空间，超出上限后单行省略并保留完整内容提示。`file.changed` 摘要按可见消息时间线展示，仅将同一连续工具批次的相邻变更聚合为单行横向滚动区域，新的助手正文或工具行之后产生的变更必须在对应位置另起一组。摘要使用与工具过程行相近的紧凑尺寸和清晰描边的展开箭头，并互斥展开一个文件的 diff。聊天区与变更侧栏共用带新旧行号、HeroUI 语义色和 Shiki 代码高亮的 Diff 渲染。聊天区 Diff 的横向与纵向滚动条都必须可见、可操作并悬浮在内容上方，不得占用布局空间；变更侧栏 Diff 不设置自身最大高度，完整展示核心内容，由侧栏容器负责纵向滚动，只在横向溢出时显示悬浮滚动条。Diff 直接复用事件中的 `path`、`diff` 和工具输入。
- 依据：用户通过参考图明确要求文件摘要横向单行滚动、一次只展开一个文件、与右侧变更栏保持一致，并确认摘要按钮的尺寸与箭头重量；后续指出同一 run 的后续修改必须出现在对应工具和正文之后，又明确纠正工具摘要应使用内容宽度和最大宽度，而不是拉伸占据剩余空间；之后要求两处 Diff 内容统一使用 Shiki 高亮且底部滚动条不占位，并进一步澄清横向滚动条不能隐藏，横向与纵向滚动条都应悬浮且不占位；本次明确变更侧栏的代码块无需内部纵向滚动，应移除最大高度并完整展示 Diff。
- 原因：以连续的可见消息作为聚合边界，可以同时保留同批文件变更的横向摘要和真实事件位置；紧凑摘要保持过程信息的次要层级，共用 Shiki 渲染则避免聊天区与侧栏在行号、颜色、语法高亮和主题适配上产生两套表现；聊天区使用双轴悬浮滚动条可控制消息高度，侧栏让外层统一纵向滚动则能避免嵌套滚动并连续查看完整 Diff。

### WEB-035

- 状态：`active`
- 范围：命令工具执行反馈与停止
- 规则：`run_command` 执行期间必须通过 `tool.updated` 区分文件状态扫描、命令执行和文件变化统计阶段，命令输出实时增量展示；存在活动工具时不重复显示“正在思考”。运行期间输入器有内容时，除发送后续消息外仍需保留独立且可访问的停止操作，停止信号同时中止扫描、命令进程和统计。
- 依据：用户指出命令长时间只显示请求内容会像黑盒，无法判断执行位置，并且输入器有内容后无法暂停。
- 原因：阶段和实时输出可以区分耗时发生在工具准备、进程本身还是收尾统计；停止操作不能因输入后续消息而消失，长时间本地副作用必须始终可控。

### WEB-036

- 状态：`promoted`
- 范围：滚动列表阴影
- 规则：需要用阴影提示滚动溢出的列表统一使用 HeroUI `ScrollShadow` 的内置阴影样式，不手写或叠加其他滚动阴影。
- 依据：用户明确要求 Thinking 滚动区域及后续带阴影的滚动列表统一使用 HeroUI `ScrollShadow`。
- 原因：组件统一处理滚动位置检测和边缘阴影，避免各列表出现不同的遮罩与阴影实现。
- 已提升至：仓库 `AGENTS.md`、`SKILL.md` 和 `project-map.md`。

### WEB-037

- 状态：`active`
- 范围：明确 UI Demo 与截图参考
- 规则：用户提供明确的 HeroUI Pro Demo 或截图时，先定位对应 Demo 源码和已安装组件导出；可直接导出的实现必须直接复用，Demo 未作为组件导出时必须以原始 Demo 组合为基线，只允许包路径、项目统一图标、产品文案和强制工程规范所需的兼容调整，不得根据截图另写一套近似实现。其他明确 UI 参考同样优先还原其列数、信息层级、内容密度、卡片字段、滚动尺寸和状态样式。
- 依据：用户指出项目看板虽然使用了 HeroUI Pro `Kanban`，但卡片内容、列结构和整体密度与指定的 Project Board Demo 差别过大；本次进一步明确要求从本地 HeroUI Pro 源码定位对应实现并保证真实复用，不能另写一套外观相同的代码。
- 原因：同名基础组件只保证原语一致，不能保证完整 Demo 的组合和信息架构一致；从已验证源码复用或以原始 Demo 为唯一组合基线，才能避免实现和参考持续漂移。

### WEB-038

- 状态：`active`
- 范围：看板拖拽反馈
- 规则：看板卡片的拖拽、聚焦与拖拽预览必须保持静止态的宽高和内容排布；键盘拖拽句柄需要可见时使用卡片内浮层定位，不能进入普通文档流撑大卡片。
- 依据：用户指出项目看板卡片拖拽时句柄进入内容流，导致卡片明显变形。
- 原因：稳定的卡片几何尺寸能让拖拽对象与落点保持一致，同时保留键盘操作的可发现性。

### WEB-039

- 状态：`active`
- 范围：项目看板布局
- 规则：项目看板左边缘必须与 Navbar 首个可见 icon 的左边缘共用相同页面内边距，右侧保留页面内边距。HeroUI Pro `Kanban` 根节点必须是真正的原生 `div` 横向滚动容器，不得只把根 `ScrollShadow` 设为禁用或 `visibility="none"`；根节点复用项目现有的可见滚动条与稳定 gutter 样式，边缘只使用窄幅渐隐层，不得用覆盖整个看板的 inset shadow。两侧渐隐层必须按首列和末列的实际可见边界显隐：首列左边缘进入容器时隐藏左侧，末列右边缘进入容器时隐藏右侧，不能只依赖 `scrollLeft`、`scrollWidth` 计算滚动条终点。不得监听 item 变化刷新阴影，也不得在外层嵌套横向 `ScrollShadow`。每列正文继续使用独立的纵向 HeroUI `ScrollShadow` 并通过 `hideScrollBar` 隐藏原生滚动条，每列限制在可用高度内并独立纵向滚动，不得让卡片撑高看板或触发页面整体纵向滚动；列状态 dot 使用 HeroUI 语义色区分 Backlog、To Do、In Progress、In Review 和 Done。
- 依据：用户先指出看板左侧超出页面内容层级、固定高度留下大块空白，并要求各状态 dot 显示对应颜色；随后进一步明确左边缘必须与 Navbar icon 左侧对齐，卡片 item 应在所属单列中滚动。横向 `ScrollShadow` 在多次拖拽修正后仍会丢失滚动条和阴影，用户最终明确要求移除这层横向阴影状态，改为手动添加两侧 shadow；后续截图进一步确认，仅把组件阴影设为 none 仍保留了错误组件结构，手写整块 inset shadow 过重且横向滚动条不可见；之后指出看板位于最左端时，左侧阴影会遮挡首列，要求起点不显示阴影，并进一步确认右侧滚动终点也需要相同处理；仅用滚动尺寸计算终点后，末列已经完整可见时右侧渐隐层仍未消失，因此改以列的实际可见边界为准。
- 原因：原生根节点直接由 `.kanban` 的 `overflow-x: auto` 承担滚动，不再经过 `ScrollShadow` 的遮罩与状态缓存；显式滚动条样式保证滑块可见，窄幅渐隐层只在对应方向存在被遮挡内容时表达溢出，列内纵向滚动继续与横向滚动分责。

### WEB-040

- 状态：`promoted`
- 范围：普通界面图标
- 规则：普通界面图标优先使用 Gravity UI Icons，只有没有语义等价图标时才保留 Lucide React。替换只改变图标组件，不改变现有尺寸、布局、HeroUI 语义色或业务状态颜色；目录选中蓝色、未选中 muted 灰色和权限 danger/warning 等状态继续由原 class 控制。同级操作显式保持一致的图标视觉尺寸与颜色；对于使用填充路径且不支持 `strokeWidth` 的图标，通过尺寸和语义色控制视觉重量，不依赖容器默认 SVG 尺寸。
- 依据：用户要求全项目优先替换为 Gravity UI 图标，并明确只替换图标、保留目录等组件已有的颜色切换；后续指出同一个加号因所在容器默认尺寸不同而显得过粗。
- 原因：统一 Gravity UI 图标语言并保留现有状态 class 能避免图标库迁移破坏业务反馈；显式统一同级图标尺寸与颜色，可以防止填充路径随缩放产生不一致的视觉粗细。
- 已提升至：仓库 `AGENTS.md`、`SKILL.md` 和 `project-map.md`。

### WEB-041

- 状态：`active`
- 范围：长会话 Turn 导航
- 规则：会话超过 3 个用户 Turn 且会话内容容器宽度不小于 48rem 时，使用 HeroUI Pro `FloatingToc` 在会话左侧展示悬浮锚点；显示条件必须基于会话容器的实际可用宽度，不使用仅反映浏览器宽度的视口断点。未悬停时所有锚点统一显示为紧凑的等长短线，并用强调色标识当前 Turn，不能通过加长破坏统一宽度。进入具体 Turn 条目后立即打开预览，并让悬停条目使用强调色、相邻条目按距离递减形成峰形；默认短线宽度与 hover 峰形宽度必须分别控制，峰形两侧仍需清晰可见。每个条目的完整纵向节距都必须可点击。预览只在条目悬停、键盘聚焦或触屏触发期间出现，同时展示该 Turn 的用户消息和最后一条 AI 正文；失败 Turn 必须显示“回复生成失败”，不得继续显示“AI 正在回复…”。用户内容最多 1 行、AI 正文或失败状态最多 3 行，卡片合计最多 4 行并在溢出时省略。预览卡片的垂直中心必须跟随对应锚点，锚点、卡片位置与预览内容切换需要过渡并尊重减少动效偏好；不同行数内容切换时使用同步淡入淡出，避免等待退出造成卡片高度和位置分两段跳变，且切换期间隐藏内容层滚动条。
- 依据：用户明确要求长会话增加 Floating TOC，悬停条目形成中间高、两侧短的峰形并高亮，只在交互时显示包含用户与 AI 回复的对应信息，切换时带过渡效果；后续进一步指定预览卡片中心跟随当前条目，并指出锚点应位于左侧且单击必须稳定跳转；之后补充默认状态不展示峰形、条目悬停时稳定显示预览、移出后所有锚点恢复为统一长度的短线，并进一步要求缩短默认锚点宽度，同时指出该调整不能让 hover 峰形缩成小点；之后明确预览卡片合计最多展示 4 行、多余内容省略，并指出 4 行与 2 行内容切换时不能出现异常过渡，进一步要求隐藏切换瞬间出现的滚动条；随后明确默认态仍需高亮当前节点；回复失败状态修复后，用户又通过窄内容区截图指出侧边导航没有随实际可用宽度适配，明确要求宽度不足时隐藏。
- 原因：紧凑锚点能在不持续占用消息宽度的情况下快速定位长会话，双向内容预览比只显示用户问题更容易确认目标 Turn；预览必须消费 Turn 的真实终态，避免失败后继续表达生成中状态。侧栏与其他可调面板会改变会话内容区宽度，容器查询可以在正文空间不足时移除导航，避免锚点压住消息内容。

### WEB-042

- 状态：`superseded`
- 范围：设置导航
- 规则：设置弹窗左侧导航的功能图标与对应文字始终使用 `foreground` 并保持同色，未选中项不弱化颜色；选中状态只通过 HeroUI 条目背景与阴影表达，不能依赖 Icon 与 `Label` 各自的默认继承色。
- 依据：用户指出设置导航中图标比文字更浅会显得别扭，要求二者同色；随后截图确认未选中项因 Icon 与 `Label` 默认颜色来源不同仍然不一致，并最终明确未选中项也应统一使用 `foreground`。
- 原因：该导航以图标和文字共同表达同一个入口，统一前景色可保持每行视觉重量稳定，而现有选中背景已经足以表达当前 section。
- 替代规则：`WEB-056`

### WEB-043

- 状态：`active`
- 范围：模型推理强度
- 规则：模型菜单只提供低、中、高三档推理强度，分别映射为 `low`、`medium`、`high`；文案依次为“响应更快、成本更低”“默认均衡”“质量更好，但更慢、更贵”。只允许选择当前模型实际支持的档位，默认使用 `medium`。模型菜单入口的当前档位保留 muted 颜色，但使用与菜单正文一致的可读字号；消息输入器的模型触发器在模型名称后、展开图标前直接显示当前档位，并随选择同步更新。输入器中的档位使用常规字重和 muted 颜色，不能与模型名称拥有相同的强调字重。
- 依据：用户明确确认三档映射、默认值及对应的质量、速度和成本说明；随后指出模型菜单入口的当前档位字号过小，并要求输入器在模型名称后直接显示推理强度；展示后又明确要求档位文字不能和模型名称一样粗。
- 原因：固定且能力感知的三档选择能保持界面简洁，并避免把 Provider 不支持的参数呈现为可用配置；在选择入口同时展示当前档位，能让关键运行参数无需打开菜单即可确认，较轻的视觉重量则能维持模型名称与附属参数之间的层级。

### WEB-044

- 状态：`active`
- 范围：模型选择长列表
- 规则：模型 Provider 的超长模型菜单必须使用与 HeroUI collection 兼容的虚拟化渲染，Provider 子菜单切换不能因一次挂载或卸载完整模型目录而阻塞交互；固定行高的模型项必须保持单行完整展示，子菜单宽度按当前 Provider 的模型名称自适应并受视口约束，不使用统一固定宽度；保留完整键盘导航和选中语义。
- 依据：用户明确指出模型列表过长时 Provider 子菜单切换卡顿，无法立即切换；虚拟化后又指出模型名称换行重叠、内容没有完整展示，并进一步纠正不同 Provider 的子菜单需要根据内容自适应宽度。
- 原因：只渲染可见模型项能把切换成本限制在稳定范围；让内容几何尺寸与虚拟列表行高一致，并以当前内容决定弹层宽度，可以避免相邻项重叠和短列表出现多余空白，同时保持 Dropdown 的无障碍交互。

### WEB-045

- 状态：`promoted`
- 范围：下拉弹层宽度
- 规则：`Dropdown`、`Select` 等下拉弹层默认按实际内容自适应宽度，不使用无业务依据的固定宽度；可以设置与内容类型匹配的最小宽度，并始终受视口最大宽度约束。虚拟列表或固定列布局等确需覆盖宽度的场景，必须根据实际内容计算。
- 依据：用户指出工作区菜单在只有单个短操作时仍使用固定宽度，并明确要求所有下拉框统一采用自适应宽度标准。
- 原因：内容驱动的宽度能避免短菜单产生无意义留白，同时让长内容在视口范围内获得足够空间。
- 已提升至：仓库 `AGENTS.md`、`SKILL.md` 和 `project-map.md`。

### WEB-046

- 状态：`active`
- 范围：侧栏工作区空状态
- 规则：工作区展开但没有会话时，“暂无对话”的文字起点必须与该工作区名称的文字起点对齐，并复用普通会话条目的统一缩进规则。
- 依据：用户通过截图明确指出“暂无对话”需要和上方文件夹名称左侧对齐。
- 原因：空状态属于工作区内容层级，沿用会话条目的统一缩进可以避免它落在文件夹图标与名称之间，破坏纵向阅读基线。

### WEB-047

- 状态：`active`
- 范围：会话中间过程折叠
- 规则：会话 Turn 的“已处理”中间过程在展开与折叠状态下共用同一左侧起点；只移除 `Disclosure.Body` 的横向 padding，必须保留组件原有的上下 padding 和纵向节奏。
- 依据：用户通过截图明确指出中间过程无论折叠与否，左侧都不需要间隙；随后纠正只应移除左边距，不能同时去掉上下留白。
- 原因：折叠状态只改变内容可见性，不应改变 Thinking、工具过程和中间正文相对 Turn 标题的阅读基线；横向对齐调整不能破坏过程项之间既有的纵向分组。

### WEB-048

- 状态：`promoted`
- 范围：消息输入器上下文与用量
- 规则：真实会话在发送按钮左侧提供紧凑的上下文占用入口，始终显示当前模型上下文占用比例；详情使用 264px 宽及紧凑字号、标记和间距的 HeroUI Popover，标题下不重复显示当前模型名称，默认只展示当前上下文，最近 Run、Session 累计和说明通过 HeroUI Disclosure 的“展开更多 / 收起”控制显示，并展示 input、output、reasoning、cache read、cache write、Token 合计和可用时的预估费用。当前上下文必须取最近一次模型请求实际携带的完整活动 Session Context，包含 System Prompt、Tool 定义和按顺序累积的消息（含 tool call/result），不得表述为单轮上下文。Web 只取最新快照表示当前窗口占用，不在多次请求之间重复累加已携带的历史。进度轨道必须按各分类相对模型上下文窗口的占比首尾相接地连续组合，并与下方分类标记颜色一致，分类之间不留轨道背景间隙，也不能只显示一个总量色块；会话消息使用 accent 蓝色，Tool 定义使用与 Trace `TOOL` 相同的 warning 橙色。分类使用请求 Context 估算，总量在 Provider 返回后用同一次请求的输入、缓存读取和缓存写入校准。详情超过可用视口高度时，必须使用限高的 HeroUI ScrollShadow 纵向滚动，不能让内容溢出弹层并遮挡输入器。推理 Token 不得在输出之外重复累加，订阅或自定义模型没有可靠金额时显示未提供。
- 依据：用户明确要求在发送按钮左侧实现参考原型中的上下文用量入口，保留上下文占用比例，并允许按项目 HeroUI 风格扩展更详细的数据；随后指出工具上下文必须像原型一样明确显示，并强调该区域按每次调用记录而不是在线累加；展示完整明细后又通过截图明确要求弹层可滚动；之后进一步指出进度条必须展示各分类的组合，不能只有单一色块，并明确纠正分类块之间不应留有间隙；本次又明确纠正“单次快照”的误导表述，要求记录整个 Session 当前上下文，其消息与工具调用必须保持原顺序；随后通过截图要求移除标题下重复显示的模型名称；本次要求会话保持蓝色，工具改为与 Trace 一致的橙色；随后要求默认只展示当前上下文，通过“展开更多”显示其余全部用量；之后指出弹层字号和间距偏大、过度占据视觉空间，最终确认保留当前字号并将面板宽度调整为 264px。
- 原因：最新请求的完整 Session Context 才能反映当前模型窗口占用；这与只统计一轮消息不同，也与把多次重复携带的历史输入相加不同。原始消息继续按顺序保存在 Session JSONL，用量快照只保存分类 Token，避免重复持久化 Context 原文；无间隙分段轨道和限高滚动继续保证分类关系与可访问性。
- 已提升至：`project-map.md` 与 `docs/架构设计.md`。

### WEB-049

- 状态：`promoted`
- 范围：文件类型图标
- 规则：所有根据文件名展示的文件图标统一通过 `components/ui/file-icon-render.tsx` 的 `FileIconRender` 渲染；组件使用按优先级匹配文件名与扩展名的策略列表，并通过 `@iconify/react` 加载本地 `@iconify-icons/vscode-icons` 图标数据。未匹配类型时保留调用入口原有的 Gravity 文件图标作为兜底，媒体附件已有缩略图时继续展示缩略图。
- 依据：用户明确要求文件 icon 全部统一使用封装后的 `file-icon-render`，采用策略模式便于维护，并保留当前 icon 作为兜底。
- 原因：单一解析入口可以避免 Markdown 链接、附件和编辑器 token 各自维护扩展名映射，同时本地图标数据符合应用的离线使用场景。
- 已提升至：`project-map.md`。

### WEB-050

- 状态：`active`
- 范围：会话变更侧栏性能与初始化
- 规则：变更侧栏默认保持关闭，关闭时不得挂载业务内容、Session Query 或文件列表，已保存的面板尺寸不得在初始化时撑开空白右栏。使用可缩放 `AppLayout` 时必须保持 aside 槽位与 `resizableAutoSaveId` 稳定，关闭态只保留无业务内容的轻量空壳，不能动态增删 `Resizable.Panel`。大量最终文件变更使用 `@tanstack/react-virtual` 只渲染可视文件行；折叠文件不得挂载 `CodeDiff` 或启动 Shiki，高亮只在用户展开对应文件后执行，并对总字符数和单行长度设置上限。构建产物等超长单行跳过高亮并只渲染带明确提示的有界预览，不得让完整内容形成超宽 DOM。净 Diff 生成和解析不得在侧栏打开时一次性覆盖全部文件，应限制到虚拟列表的可视行。
- 依据：用户通过大数据量真实会话指出打开变更侧栏明显卡顿，并明确要求文件列表虚拟滚动、Shiki 延迟到文件展开后渲染、侧栏关闭时不加载内容，同时修复初始化自动打开空面板的问题；随后又通过构建产物 Diff 截图指出点击展开会直接卡死。
- 原因：关闭态卸载业务内容可以消除无效 Query 与渲染，同时稳定面板约束能避免 `react-resizable-panels` 在打开时找不到动态加入的 Panel；文件级虚拟化和按需、有界高亮把打开与展开成本限制在当前视口和安全内容规模内，避免文件数量、Diff 总体积或压缩后的超长单行阻塞主线程。

### WEB-051

- 状态：`active`
- 范围：变更侧栏文件行
- 规则：文件行不显示 A/M/D 状态字符；文件信息保持非交互展示，动态文件名只能占用剩余空间并单行省略，尾部的展开与系统打开操作固定保留、不参与收缩，使用同尺寸、同语义色和同 HeroUI `ghost` 反馈的独立图标按钮；已删除文件仍保留禁用态的系统打开按钮，不让内容或状态改变操作区布局。
- 依据：用户通过截图指出开头状态字符冗余，尾部两个图标视觉不协调且交互反馈不一致；后续进一步指出动态文件名不能影响尾部两个按钮的展示。
- 原因：文件图标与增删数字已经表达文件类型和变更结果；同级操作使用一致且稳定的按钮区域，可以避免整行 hover 与单个图标 hover 混用，也能防止不同路径长度或文件状态引发布局跳动。

### WEB-052

- 状态：`active`
- 范围：Session 切换加载状态
- 规则：切换 Session 时立即提交目标路由并更新选中态；保留 Session route 的即时 `pendingComponent` 和快照 loader，让未缓存目标立刻显示骨架。loader 与页面或变更面板共享可取消 Query 时，正常的 Query 取消不得进入错误页；仅在当前导航仍有效时重新获取，已经离开的导航直接结束。骨架必须复用当前会话的结构和几何尺寸，展示右侧用户气泡、左侧无头像助手正文和底部输入器，不使用头像加长文本行的通用模板。已有可用缓存时直接展示目标 Session，不强制闪现骨架。
- 依据：用户明确要求 Session 立即切换并在数据请求期间使用骨架屏，随后通过实际界面截图指出通用头像式骨架不符合当前项目格式；慢速网络暴露出共享 Query 取消被 Router 当成错误的问题，并明确纠正不能通过移除即时路由骨架规避该问题。
- 原因：即时 pending route 同时保证侧栏选中态和目标加载态及时反馈；区分导航取消与真实加载失败，可以保留骨架交互并避免正常竞态落入错误页。

### WEB-053

- 状态：`active`
- 范围：页面级未捕获错误
- 规则：Router 必须配置符合项目视觉的全局错误页，不使用 TanStack Router 默认的英文调试错误页面，也不向用户直接展示原始异常文本或堆栈。错误页使用项目组件提供简短中文说明、重试和返回首页操作。
- 依据：用户指出所有错误都落入 `Something went wrong / Hide Error` 默认页面，视觉不符合项目且难以恢复。
- 原因：统一的安全错误状态能避免泄露内部异常细节，并给用户稳定的恢复路径。

### WEB-054

- 状态：`active`
- 范围：会话 Turn 文件变更入口
- 规则：每个 Turn 的净文件变更只在该 Turn 最终回复与消息操作之间显示；没有原始变更时不渲染。摘要底色使用 HeroUI 页面 `background` 与 `surface-secondary` 的中间层级：需要比页面背景稍深，但比原始 `surface-secondary` 更淡，并继续随主题变量变化；作为嵌入消息流的次级表面不使用 Card 默认阴影。顶部右侧使用不带 Tooltip 的文字加图标按钮：审核打开仅包含该 Turn 变更的侧栏，文件项直接打开并定位对应 Diff；撤销成功后摘要与原始 Diff 继续保留，操作切换为“重新应用”，重新应用后再切回“撤销”，两种成功状态都使用 Toast 反馈。文件列表默认展示 3 项，文件名使用常规字重，不继承 Button 的中等字重；多余项通过“再显示 N 个文件”按需展开，展开后保留“收起文件”操作恢复三项。摘要内按钮保留 HeroUI 颜色反馈，但不使用按压缩放。撤销和重新应用都基于该 Turn 的 before/after 快照恢复文件，冲突或失败使用 AlertDialog 明确反馈；不提供脱离 Turn 上下文的全局变更入口。
- 依据：用户明确要求多次 Turn 的变更分别展示，并进一步指定顶部操作、文件级 Diff 定位、三项折叠和撤销失败反馈；随后要求移除点击缩小效果、支持重新收起，并在撤销后提供可重新应用的操作及 Toast；顶部撤销/重新应用和审核均指定为文字加图标且不使用 Tooltip；之后先指出 `surface-secondary` 过深，又明确纠正默认 `surface` 太白，要求取页面背景与旧版之间的较淡深色，并指出 Card 阴影使摘要显得突兀；本次进一步要求文件名条目不使用加粗字重。
- 原因：Turn 级入口让文件变化与产生它的回复保持关联；介于背景和次级表面之间的主题色配合无阴影表面能保持摘要边界，同时避免形成悬浮卡片般的过强视觉聚焦；保留原始 Diff 并把应用状态做成可逆操作，能够持续提供审核依据；可逆的渐进展示和稳定的按钮、文件行尺寸能降低长列表对消息阅读的干扰与点击时的视觉跳动。

### WEB-055

- 状态：`active`
- 范围：消息输入器 Skill 调用
- 规则：保留“+ → Skills”作为图形化显式调用入口，菜单以单一列表展示只读系统 Skill，以及当前 Workspace 的真实全局与项目 Skill，并在条目尾部标注“系统/全局/项目”；列表为空时显示统一空状态，加载异常时显示错误状态。选中后在编辑器中保持 Skill 标签，提交给 daemon 时序列化为 `$skill-name`；会话中的用户消息与助手 Markdown 必须把符合 Skill 名称语法的 `$skill-name` 恢复为同一紫色图标标签，代码与链接保持原语义。输入器和消息区的 Skill 标签必须继承所在文本行高，由文字建立与相邻正文一致的外层基线，图标单独在行盒内垂直居中；长 Skill 子菜单从触发行向上展开，底边与主菜单底边对齐；不在输入器中增加重复的第二套 Skill 入口。
- 依据：用户确认输入器已有“+” Skill 选择能力，并要求将它接入 Codex 风格的显式 Skill 调用；随后通过截图指出序列化后的用户消息不应退化为纯文本，并要求助手回复中的 Skill 引用使用相同展示；展示后进一步指出输入器与消息中的标签行高不一致，导致内容没有垂直居中，并要求长 Skill 子菜单从触发行向上展开；之后截图进一步指出图标和文字基线问题；本次通过 Codex 截图明确要求 Skill 选择同时包含系统内置 Skill，并显示系统来源。
- 原因：复用现有标签交互和扁平目录可以保持输入器简洁，而真实数据、统一空状态与错误状态能让选择不依赖 mock 或模糊匹配；消息端恢复同一视觉语义可以让显式调用在提交前后保持可辨识。`inline-flex` 的首个 SVG 会参与容器基线计算，容器应由文字建立基线以对齐相邻正文，同时让图标通过独立交叉轴对齐在行盒内居中，避免图标被文字基线向上抬高。子菜单向上展开并贴齐主菜单底边可避免长列表压住输入器，同时保持两级菜单的视觉锚点稳定。

### WEB-056

- 状态：`active`
- 范围：设置导航
- 规则：设置弹窗左侧导航的文字使用 `foreground`，功能图标使用 `muted`，不让图标与文字同色；选中状态继续只通过 HeroUI 条目背景与阴影表达。
- 依据：用户通过设置导航截图明确纠正图标和文本颜色不应一致。
- 原因：弱化图标可以保留文字作为主要识别信息，并形成稳定的视觉层级。

### WEB-057

- 状态：`superseded`
- 范围：设置中的技能管理
- 规则：设置中的技能管理只展示全局 Skill，项目 Skill 继续由当前 Workspace 的 Agent 与消息输入器使用，不混入全局设置列表。技能列表每项只提供可用状态开关与卸载，不展示安装状态、scope 标签或由资源文件派生的标签；所有技能统一使用紧凑的技能语义图标，图标进入详情；列表通过无背景的蓝色 Button 展示可点击打开的绝对技能目录，目录使用 12px 字号，独占主信息行下方的一行，与主信息保持 8px 间距，并按图标的可见边缘与技能图标容器左侧对齐，不与描述或尾部操作争夺同一行空间。插件市场分类与技能状态筛选使用一致的独立按钮样式，选中项统一使用柔和的蓝色 `accent-soft` 语义色；技能页不显示独立搜索框；技能列表项与插件市场列表项共用同一组件结构、间距、圆角、图标容器、信息密度和整行交互样式。
- 依据：用户明确要求设置中的技能移除安装概念和资源标签，统一图标，并通过绝对路径、详情和文件夹打开操作管理技能；随后指定状态筛选原样复用插件市场的分类按钮，仅调整激活色，并要求技能列表项保持与插件市场列表项一致的外观；进一步要求缩小技能图标，并让目录采用添加 Provider 操作的蓝色前景风格，同时保持透明背景；之后又明确指出技能搜索和列表项观感不佳，要求完整复用插件市场列表项样式；随后进一步澄清设置中的技能只用于全局 Skill，项目 Skill 不应显示；接着通过截图明确要求目录链接移到主信息行下方；之后指出目录字号过大且与主信息不协调，要求使用 12px；随后进一步澄清目录左侧应与技能图标左边缘对齐；截图确认文件夹图标的可见边缘仍偏左且上下距离过近；本次对比两种筛选样式后，要求先将插件市场改为与技能页一致的柔和蓝色选中态。
- 原因：全局设置只管理跨 Workspace 的 Skill；项目 Skill 仍保留在共享目录接口和 Agent 发现链路中，按具体 Workspace 使用。直接展示路径和少量真实操作比安装态与派生装饰更准确；分离模式可消除连体按钮的直角边，同时维持一致的状态语义。
- 替代规则：`WEB-058`

### WEB-058

- 状态：`active`
- 范围：设置中的技能管理
- 规则：技能页默认选中“全部”，其余筛选项按已登记项目排列，不再按开启状态筛选；只有“全部”视图按来源分组，依次展示只读系统 Skill、全局 Skill和各项目 Skill，只渲染有实际条目的来源分组。系统 Skill 使用“系统”尾部标记，不提供开关、目录或卸载操作；项目筛选视图只展示对应项目 Skill，不重复分组或混入系统与全局 Skill。项目 Tab 名称必须设置固定最大宽度并单行省略，Tab 行使用限于技能面板宽度的 HeroUI 横向 `ScrollShadow`，保持不换行并独立横向滚动，不得撑宽或带动整个设置页面横向滚动。“全部”视图的各来源分组标题必须复用同一结构和样式：使用紧凑的小号中等字重、相同图标尺寸，标题与列表保持紧凑间距，组间使用稳定的 16px 节奏；分组标题本身只展示信息且不可点击，有本地目录的分组在标题行尾使用小号 HeroUI ghost 文件夹按钮打开对应 Skill 根目录，完整绝对路径只放在该按钮的 Tooltip 和无障碍名称中。全局分组使用全局 Skill 根目录，每个项目分组必须使用该项目实际 Skill `directory` 的父目录（即对应项目的 `.agents/skills`），不得退化为项目根目录。分组列表不使用常驻背景或外层 secondary `Surface`，每个技能 item 默认透明，只在自身 hover 或键盘聚焦时显示 HeroUI 背景反馈；技能 item 不再展示底部目录行，继续与插件市场列表项共用同一结构、间距、圆角、图标容器和整行交互样式。列表中的卸载操作使用 HeroUI `danger-soft` 降低重复危险按钮的视觉重量，最终确认弹窗仍使用实心 `danger`。技能详情不使用弹窗，点击技能后在设置内容区切换为详情页，并与插件市场详情复用完全相同的返回、图标、标题、描述、操作和内容间距布局；详情按需加载并展示 `SKILL.md` 正文，不显示“所在目录”区块或独立的 `SKILL.md` 标题。可管理 Skill 的标题行右侧使用“已开启/已关闭”文字加开关，顶部导航行右侧放置打开目录和 `danger-soft` 卸载操作，卸载继续复用列表的确认流程。
- 依据：用户通过插件市场、已归档项目标题和现有技能路径截图，明确要求技能 item 以插件市场为准；“全部”按全局与项目分组，其他 tab 按项目排列；并把列表 item 底部的路径交互提升到项目分组。后续截图指出超长项目名会撑宽整个设置页，明确要求项目 Tab 具有最大宽度且 Tab 行独立横向滚动；随后进一步指出“全部”视图的间距和字体层级不协调、整体展示杂乱，要求按 HeroUI 风格统一整理；之后要求项目与全局标题下方显示小号目录路径，并进一步明确只有路径可点击、标题不可点击；随后指出各项目必须定位到该项目具体的 Skill 目录，不能打开项目根目录；之后明确技能 item 的灰色背景只能在 hover 时显示；最新截图又确认常驻绝对路径造成页面杂乱，并接受改为标题行尾小号打开目录按钮、完整路径仅在 Tooltip 展示的方案；之后明确技能详情应移除弹窗，并与插件市场详情保持完全一致的整体布局样式；随后要求详情展示 `SKILL.md` 内容并移除“所在目录”；通过截图明确要求移除正文上方独立的 `SKILL.md` 标题；之后要求详情页提供开启或关闭和卸载功能；之后明确状态由“已开启/已关闭”文字和开关共同展示，并将目录与卸载放置在顶部导航操作区；随后澄清“全部”视图不应为每个空来源重复显示空状态；本次通过 Codex 截图要求“全部”中增加只读系统 Skill，并显示系统来源。
- 原因：来源导向的筛选与分组能直接表达系统、全局和项目 Skill 的实际作用域；只渲染有条目的来源可以避免重复空状态，系统尾部标记则能在不提供无效管理操作的情况下说明其内置属性。同级分组复用同一紧凑标题行，并把低频目录信息放入操作 Tooltip，可以消除超长项目名在标题与绝对路径中的重复，同时保留准确的目录入口和键盘可访问性。从已发现 Skill 的真实目录反推项目 Skill 根目录，可避免在 Web 拼接平台相关路径或错误打开项目根目录；列表背景只表达当前交互目标，也与共用的插件市场 item 状态保持一致。技能与插件详情复用同一布局组件，可以从结构上保证宽度、间距和信息层级一致，避免两套实现继续漂移；正文按需加载可避免技能目录接口向列表和消息输入器传输全部内容，直接使用正文首个标题可以避免重复层级；可管理 Skill 继续复用同一 mutation 和确认弹窗，系统 Skill 保持只读。

### WEB-059

- 状态：`active`
- 范围：项目任务看板命名
- 规则：项目任务看板统一使用 `board` 作为路由、页面文件和页面类型命名。
- 依据：用户明确要求路由和代码名称与看板功能保持一致。
- 原因：路由和代码命名应直接表达页面职责，避免导航、类型与页面实现语义漂移。

### WEB-060

- 状态：`active`
- 范围：设置中的技能新增流程
- 规则：技能页标题区使用与模型页“添加 Provider”一致的柔和次级胶囊按钮作为“新增技能”菜单触发器，菜单提供“创建技能”和“安装技能”。操作目标优先使用当前项目 Tab，处于“全部”时使用当前会话或新会话所选项目；没有项目时禁用入口。创建关闭设置并进入目标项目的新会话，在输入器中插入可见的系统 `Skill Creator` 标签和待补充需求，不自动发送。安装不进入输入器，使用紧凑的 HeroUI Modal，只保留必填的 GitHub 地址、可选 Skill 名称、错误状态和操作按钮；不显示范围选择器、顶部目标说明或字段下方重复描述，且始终安装到全局技能目录。Web 不接收命令文本，daemon 只以参数数组运行固定版本 Skills CLI，在临时目录下载并校验后复制到经过真实路径检查的全局目录，同名拒绝覆盖，并具备中止、超时、输出限制与失败清理。
- 依据：用户要求技能管理增加新增按钮，区分创建和安装；创建需要进入当前项目的新会话并在输入框使用创建技能能力，安装需要支持类似 npx 命令的本地安装方式；随后通过截图明确要求新增按钮复用模型页“添加 Provider”的按钮风格；进一步要求创建像 Codex 一样在输入器中加入系统 Skill 标签，并明确安装必须使用弹窗而不是进入输入框；本次纠正安装只能进入全局目录，表单只需 GitHub 地址和可选名称，并要求减少不符合 HeroUI 风格的冗余描述。
- 原因：创建仍需会话承载开放式需求，但系统 Skill 标签必须对应 Runtime 可加载的真实内置能力；安装属于字段明确的一次性工作流，固定全局范围后无需再让用户选择或阅读重复说明。固定 CLI 与受限参数避免把任意 Shell 暴露给 Web，临时校验和路径检查让全局写入保持可控。

### WEB-061

- 状态：`promoted`
- 范围：消息附件与 `@` Workspace 上下文
- 规则：附件和 `@` 引用必须作为同一条用户消息的结构化输入提交，并在 Context 预算、裁剪与压缩之前完成组装，不得改成 Tool Call。附件选择不使用只允许图片和文本的扩展名白名单，所有普通文件都允许附加：图片继续使用模型图片输入，可解码文本直接读取，PDF、Office、OpenDocument、RTF 与 EPUB 等常见文档在 Runtime 本地提取文本，无法解析的其他二进制文件保留名称、MIME 类型和大小并明确标记正文未展开，不把二进制或 Base64 原样注入 Context，也不因此拒绝整次 Run。输入 `@` 时使用当前 Workspace 的真实图片、文件和文件夹候选，选中项在编辑器和历史消息中保持同一内联标签，并对文件引用复用同一文档解析与二进制降级策略。`@` 候选搜索必须预建可复用的规范化搜索文本，避免每次按键重复转换全部候选；长候选菜单必须使用兼容 HeroUI Collection 的虚拟化，只挂载可视行和对应文件图标，并保留现有分组、选中状态与键盘交互。动态候选和分组必须通过 Collection 的 `items` 数据源驱动，不能仅把静态 Section 列表包进 Virtualizer；图片、普通文件和文件夹分组必须能在同一轮结果中完整出现，不能因虚拟化只保留第一个非空分组。模型上下文中的附件与引用必须标记为不可信参考资料，并执行数量、大小、文本截断、目录清单、解析中止、解压上限、模型图片能力与 Workspace 真实路径限制；消息恢复后仍展示原始正文、标签和附件元数据。
- 依据：用户要求完成添加附件与 `@` 引用上下文，并明确确认该能力应在 Context 实现前完成；提供的 Codex 截图用于说明 `@` 候选按图片、文件和文件夹分组的交互参考；随后明确反馈只要输入 `@` 或继续输入部分文件名就会因候选文件较多而卡顿，并要求引入虚拟滚动；之后明确纠正 PDF 等其他格式不应被附件入口拒绝，文件类型应普遍支持；本次进一步指出 `@` 搜索结果只出现图片、无法找到普通文件，并要求确认 `@` 引用仍是请求前 Context 组装而非 Tool Call。
- 原因：先把用户输入收口为完整、可恢复的结构化消息，后续 Context Pipeline 才能对实际发送内容统一计量、裁剪和压缩；数据边界与路径限制可避免附件内容提升指令优先级或绕过 Workspace 安全边界。底层模型消息只具备文本与图片内容类型，因此通用文件支持必须由 Runtime 在本地提取可用文本，并为未知二进制提供不误导的元数据降级；解析限额和中止信号避免不可信复杂文档无限消耗资源。文件图标和列表项的全量挂载是打开 `@` 时的主要同步成本，预建搜索文本与 Collection 虚拟化可以在不缩减可搜索范围的情况下减少每次按键计算和 DOM 挂载；以动态 Collection 数据源驱动分组可以在异步加载和搜索更新后让虚拟布局得到完整集合，避免静态分组缓存造成候选类型丢失。
- 已提升至：`project-map.md` 与 `docs/架构设计.md`。

### WEB-062

- 状态：`active`
- 范围：用户消息中的附件展示
- 规则：继续使用 HeroUI Pro `ChatAttachment` 展示历史消息附件；普通文件附件采用紧凑的弱层级样式，使用较矮的内容高度、`surface-secondary` 背景、无装饰性描边和光学放大的文件图标，避免比相邻用户正文气泡更抢眼。图片与视频附件保持组件默认缩略图布局，不套用普通文件卡片的尺寸覆盖。
- 依据：用户通过真实会话截图指出普通文件附件卡片不像 HeroUI 风格，并确认按紧凑、弱边界方案调整。
- 原因：附件属于用户消息的补充信息；保留 Pro 组件语义与媒体布局的同时降低普通文件卡片的视觉重量，可让正文继续成为消息主层级。

### WEB-063

- 状态：`active`
- 范围：会话工具调用图标
- 规则：单个工具调用和工具组摘要必须在状态文案前显示与工具语义对应的 Gravity UI 图标，并统一尺寸与 muted 语义色。图标选择集中在 chat feature 的单一解析入口：优先按稳定工具名精确映射，再按名称语义匹配外部或未来工具，最后使用通用工具图标兜底；Skill、命令、文件读写、目录、搜索、图片和 Web Search 等类别不得在各消息组件中重复写条件判断。
- 依据：用户通过 Codex 与当前项目截图对比，明确指出工具调用前缺少图标，要求覆盖 Skill、命令等现有工具，并让后续 Web Search 图标方便扩展。
- 原因：集中映射可以让同一工具在单项、分组和未来展示入口中保持一致，同时避免新增工具时修改多个渲染组件或出现尺寸、颜色漂移。

### WEB-064

- 状态：`active`
- 范围：设置中的技能加载错误
- 规则：技能页按 Workspace 加载技能失败时，错误提示必须以失败的 Workspace 名称作为标题，并在下方显示安全的后端错误消息；“全部”视图存在多个失败 Workspace 时分别展示，不能只显示无归属的通用错误标题。
- 依据：用户指出仅显示“技能加载失败 / Workspace 操作失败”无法判断具体失败项目，明确要求展示 Workspace 名称及其错误。
- 原因：技能目录按 Workspace 独立请求，错误反馈保留同一归属关系才能让用户定位失效、移动或不可访问的项目。

### WEB-065

- 状态：`active`
- 范围：项目评测代码与命令
- 规则：Context Runtime、daemon 恢复、Web 协议联调等可重复评测的入口、场景和 fixture 统一放在 `packages/eval`；被评测模块只提供生产代码本来需要的最小公开能力，不在 `apps/*` 或其他 `packages/*/src/evals` 分散维护评测实现。
- 依据：用户明确要求所有涉及评测的内容统一写入 `packages/eval`。
- 原因：集中评测可以让本地合成场景、JSONL 重启恢复和真实 Provider burn-in 使用一致命令与依赖边界，避免评测脚本跟随单个运行时包漂移。

### WEB-066

- 状态：`active`
- 范围：上下文 Checkpoint 管理
- 规则：从上下文用量 Popover 打开 Checkpoint 管理 Modal 时，必须先关闭受控的父 Popover，不能让两个 overlay 同时保留；管理 Modal 同时提供右上角关闭入口和底部关闭操作。Modal Header 只保留 HeroUI `Modal.Heading`；作用说明放在 Body 首段，使用 Body 默认正文层级、限制阅读宽度并与后续控件保持 16px 间距，简要说明 Checkpoint 是每次上下文压缩后保留的工作摘要，切换会改变后续模型上下文。主操作使用“将对比版本设为当前”，运行中禁用文案使用“Run 结束后可切换到对比版本”，必须明确操作目标是对比版本，不使用依赖左右方位的文案，也不把切换工作摘要误写成恢复代码或历史。Checkpoint 已存在时，管理入口使用带明确文案的 HeroUI secondary Button，不使用难以发现的 ghost 短文本。版本对比按目标、约束、决策、已完成、待处理、阻塞和下一步逐项分区，同一字段的基准值与对比值并排对齐；字段正文统一复用 `AssistantMarkdown` 渲染，数组内容保持逐条分组并支持行内代码、链接和基础 Markdown；长路径和符号名必须在列内换行，不能撑开双栏或产生横向滚动。顶部版本选择器与下方内容卡片必须共用完全相同的双栏宽度和中线，滚动条不得挤占内容区导致列边缘偏移。
- 依据：用户先通过真实界面指出 Checkpoint 管理弹窗与上下文 Popover 同时存在导致无法正常关闭，并明确反馈原“管理”按钮不明显；后续截图又指出双栏内容被长符号名撑开，所有工作状态字段连续堆叠、不便查看；分区后再次明确指出滚动内容与顶部版本选择器的列中线没有对齐，并要求字段内容按 Markdown 渲染；理解其作用后进一步要求把“恢复对比版本”改为准确的切换文案，并在弹窗中直接说明该功能；加入说明后又通过真实界面明确指出把字段 `Description` 直接放进 Modal Header 不符合 HeroUI 弹窗风格；随后明确指出“设为当前 Checkpoint”没有说明操作目标是基准版本还是对比版本。
- 原因：互斥 overlay 可以避免层级与焦点争抢；明确的次级操作和两个标准关闭路径能提高管理能力的可发现性与可恢复性；按字段对齐的分组对比可以保持版本关系，并避免长内容破坏弹窗布局；统一列宽基准可避免滚动容器改变视觉中线。

### WEB-067

- 状态：`active`
- 范围：自定义 Provider 模型弹窗
- 规则：弹窗标题已经表达当前 Provider 的模型语义，不再重复显示可见的“模型列表”标题；Fieldset 保留仅供无障碍使用的分组名称，模型卡片直接承接弹窗正文，卡片列表与新增区使用稳定的 16px 间距。多个模型卡片使用单行 HeroUI 横向 `ScrollShadow` 浏览，隐藏原生滚动条并保留边缘渐隐、触控和键盘滚动，不在弹窗纵向滚动外再嵌套模型列表纵向滚动。横向滚动边界必须止于模型卡片容器；外层 Form、Fieldset 与新增模型表单保持可收缩且占满弹窗可用宽度，不得被卡片的最小内容宽度撑开或参与横向滚动。自定义模型为空时不显示“暂无模型”，直接以“模型 ID”输入作为首要内容；已有模型时新增字段使用“新增模型 ID”。
- 依据：用户先通过截图指出空列表标题与“暂无模型”组合观感差，随后指出已有模型时“模型列表”标题造成的垂直间距同样不协调；模型较多时又要求改为横向浏览，随后明确纠正只有模型卡片可以横向滚动，底部新增表单不能随之溢出。
- 原因：弹窗标题和紧邻的新增入口已经表达列表语义与下一步；移除重复层级并统一内容区间距，可以避免标题上下使用两套不均衡的留白。模型卡片具有固定的横向字段结构，单行横向浏览既能保持卡片可读宽度，也能消除嵌套纵向滚动条；限制 flex 子项的最小内容宽度可以把溢出封装在卡片列表内，保持底部表单和弹窗操作稳定。

### WEB-068

- 状态：`active`
- 范围：会话上下文压缩状态
- 规则：自动上下文压缩必须作为当前 Turn 的中间过程进入思考链；开始时显示“正在自动压缩上下文”，完成后原位切换为“上下文已自动压缩”，Turn 结束后随其他中间过程一起折叠。
- 依据：用户通过 Codex 状态截图明确要求上下文压缩状态也加入思考链。
- 原因：压缩会暂停主模型输出；把其开始与完成状态放回真实事件顺序，可以解释等待时间并保持长任务过程可追溯。

### WEB-069

- 状态：`active`
- 范围：上下文与用量 Popover
- 规则：活动 Checkpoint 的当前目标与下一步必须使用独立标题和内容分组，不能与 Token、生成方式等指标混排成连续弱化文本；底部统计说明按当前上下文、估算方式、累计范围和费用说明等语义拆分为独立段落，不使用整块长文。
- 依据：用户通过真实界面截图指出 Checkpoint 摘要展示不明显且内容杂乱，并明确要求底部描述按语义分段。
- 原因：Checkpoint 是压缩后继续任务的核心工作状态，需要比普通指标更明确；分段说明可以降低紧凑 Popover 中长文本的扫描成本。

### WEB-070

- 状态：`active`
- 范围：上下文 Checkpoint 回退语义
- 规则：切换到历史 Checkpoint 后，后续模型热上下文必须从该摘要和恢复操作之后的新消息继续，不得把被回退版本之后、恢复操作之前的消息重新自动拼回请求。完整 Session 消息仍保留用于界面展示、审计和显式历史检索。
- 依据：用户在真实会话中切换 Checkpoint 后，模型回答仍使用最后一次内容，明确指出回退没有生效。
- 原因：只切换摘要引用而继续携带后续消息会立即抵消回退效果；恢复边界必须同时约束请求前的消息投影，而不能删除 Session 事实。

### WEB-071

- 状态：`promoted`
- 范围：输入器上方 Plan/Todos 工作状态
- 规则：只展示活动 Run 中非空的最新 Plan/Todos，Run 完成、失败或中止后立即隐藏；输入器上方使用左右双栏，左侧 `Plans`、右侧 `Todos`，两栏分别还原 assistant-ui `AgentPlan` 与 `TodoList` 的标题、计数、进度和条目状态，不共享或合并进度，并各自使用独立的 HeroUI `ScrollShadow`。工作状态使用 HeroUI `Disclosure`，默认折叠为带类型图标和独立语义色的两组 `Chip` 进度摘要，摘要上下边缘留白一致，展开后显示完整双栏内容；进行中状态使用简洁的强调色 Loader，并继续在原位更新而不加入思考链。
- 依据：用户先要求工作状态可折叠且终态后隐藏，之后明确纠正 Plans 与 Todos 都应回到输入器上方左右展示、进度分开并还原 assistant-ui 官网组件；后续截图进一步指出两栏必须分别滚动、进行中图标需要替换，折叠摘要需要符合 HeroUI 的图标和颜色表达；本次明确要求默认折叠，并指出摘要上下边缘留白不一致。
- 原因：并排保留两个官方组件的完整信息结构，可以同时观察长期计划与即时执行清单；独立滚动避免长列表互相牵连，默认折叠与均衡留白则让持续显示的工作状态减少对输入区的挤占。
- 已提升至：`project-map.md` 与 `docs/架构设计.md`。

### WEB-072

- 状态：`active`
- 范围：探索页
- 规则：产品不提供独立探索页、探索导航入口或 `/explore` 路由，不为组件演示保留面向用户的页面和 mock 会话数据。
- 依据：用户明确要求移除探索页面及其路由。
- 原因：组件演示不需要占用正式产品的信息架构和路由空间。

### WEB-073

- 状态：`active`
- 范围：未知路由
- 规则：产品不展示 404 页面；所有未知 Web 路由和不存在的 Session 路由统一重定向到 `/new`。单段未知地址会先匹配 Session 动态路由：不符合 UUID 约束时 daemon 返回 400，符合格式但不存在时返回 404，Session loader 必须将两者都直接重定向；其余未知地址由 Chat Layout 下的通配路由接管。两者都复用 Chat Layout 的认证守卫，未登录时先进入已注册的 `/login`，不得依赖 Not Found 渲染组件发起二次导航。
- 依据：用户明确要求未知路由进入新会话页，并特别要求未登录场景避免无限重定向；首次仅在根 Not Found 组件中渲染 `Navigate` 未能处理实际匹配为 Session 的单段未知地址，用户反馈没有生效，随后进一步要求移除 404 页面。
- 原因：在 loader 400/404 和显式通配路由中直接抛出重定向，可以覆盖格式无效、Session 不存在与真正未匹配三条路由分支；认证继续由共同父路由处理，避免重复判断和重定向环。

### WEB-074

- 状态：`active`
- 范围：会话搜索
- 规则：会话搜索必须覆盖标题、由最近可见消息形成的描述摘要，以及 JSONL 中所有可见用户与助手消息；查询由 daemon 在事实源上完成，Web 只接收命中 Session、有界摘要和实际命中的消息事件 ID。搜索弹窗复用 HeroUI Pro `Command` 的输入、列表、空状态与键盘导航，结果项展示标题、命中摘要和更新时间，不在 Web 批量加载完整会话后自行扫描。Web 在防抖查询与当前输入一致前不复用上一轮结果，Command 不对服务端结果做隐式二次筛选；消息命中摘要必须把查询文本放在单行可见前段并保留少量前文，标题和摘要中的查询文本使用 HeroUI 强调语义色高亮。点击消息命中结果后，通过瞬时 UI 状态进入会话并让虚拟列表定位到对应消息，不把定位参数写入 URL；命中关键词以语义强调背景呼吸高亮 3 次后平滑消失，减少动态效果偏好下降级为单次淡出。
- 依据：用户明确要求搜索不能只命中标题，还要覆盖描述与对话消息，并要求弹窗符合 HeroUI 风格；随后指出搜索词与可见结果不一致并要求高亮实际命中部分，又明确纠正摘要必须直接展示命中的关键内容；最后要求点击后定位到具体消息，关键词背景呼吸高亮 3 次后过渡消失，且 URL 不携带参数。
- 原因：由 daemon 读取 JSONL 可以保持消息事实来源单一，同时避免 Web 为搜索下载所有会话内容；复用 Command 能统一弹层与键盘无障碍交互。只展示与当前输入同步的服务端结果，并让关键词出现在实际可见的摘要前段，可以避免旧查询、隐藏文本或单行截断造成无法解释的结果；事件 ID 提供稳定定位，瞬时状态保持 URL 可分享且无一次性交互残留。

### WEB-075

- 状态：`active`
- 范围：Trace 详情响应式布局
- 规则：Trace 详情在桌面端保留右侧面板；移动端不占用列表横向空间，点击轨迹记录后使用 HeroUI 底部 Drawer 展示同一详情内容，并支持遮罩、拖拽和关闭操作退出。Drawer 外层与详情内容必须使用同一背景 token；内容允许铺满宽度，但拖拽把手必须保留顶部内距，不能贴住或越过圆角顶边。移动端 Trace 主内容左右统一使用 `px-4`，桌面端保留现有非对称布局。
- 依据：用户通过移动端截图指出固定右侧 Trace 详情挤压并溢出页面，明确要求点击 item 后在下方抽屉展示；实现后进一步通过截图指出抽屉顶部结构、上下区域背景色和移动端左右间距不一致。
- 原因：详情属于按需钻取内容；小屏覆盖层可以保留轨迹列表宽度，同时复用同一详情组件避免两套内容与状态漂移。

### WEB-076

- 状态：`active`
- 范围：移动端消息输入器选择器
- 规则：消息输入器的权限审批和模型触发器在移动端只展示当前选项图标，并保留可访问名称与 Tooltip；模型弹层从触发器上方起始端对齐，让弹层左边缘从模型触发器开始，按内容收缩并保留合理最小宽度与视口上限，尾部状态与展开箭头不得被两个自动间距拆散。Provider 在同一弹层内折叠展开，不打开二级子菜单；一次只展开一个 Provider，长模型列表继续虚拟化滚动，展开的 Provider 标题吸附在其滚动容器顶部。模型和推理强度使用 `ListBox` 的选择事件更新受控选中项，不沿用桌面 `Dropdown` 的 action 事件；桌面端继续使用现有名称触发器与 Provider 子菜单。
- 依据：用户先通过移动端截图指出权限与模型名称挤出输入器，并明确要求 Provider 在移动端原地折叠展开、长列表滚动时标题吸顶，而不是继续打开二级菜单；实现后再次指出弹层内部位置错误、宽度过大且模型无法勾选，随后进一步要求弹层左侧与模型触发器对齐。
- 原因：小屏工具栏需要把有限宽度留给输入与主要操作；从触发器起始端锚定可避免弹层因尾端对齐向视口左侧漂移，内容宽度和单一尾部间距能让临时弹层保持紧凑、对齐，`ListBox` 选择语义可以同时驱动点击、键盘与选中指示；同层展开、吸顶与虚拟化共同保持长模型目录的上下文和交互性能。

### WEB-077

- 状态：`active`
- 范围：Trace 顶部工具栏响应式布局
- 规则：移动端 Trace 顶部统计信息保持紧凑单行，空间不足时使用 HeroUI 横向 `ScrollShadow` 浏览；搜索框独占下一行并铺满可用宽度。较宽视口恢复统计与搜索同一行，搜索框保持桌面宽度约束。
- 依据：用户通过移动端截图指出统计信息与受桌面最大宽度限制的搜索框组合观感不佳，明确要求参考 HeroUI 风格调整移动端展示。
- 原因：固定的两层移动布局可以避免搜索框形成无意义的半宽空白；仅在统计项真实溢出时横向浏览，可保持信息完整而不增加换行高度。

### WEB-078

- 状态：`promoted`
- 范围：移动端二级菜单
- 规则：移动端弹层中的二级菜单统一在当前弹层内使用 HeroUI `Disclosure` 折叠展开，不横向打开独立子弹层；同组二级入口一次只展开一个，子项继续使用对应 Collection 组件保留点击、键盘和无障碍语义。桌面端继续使用 `Dropdown.SubmenuTrigger`。
- 依据：用户先要求移动端模型 Provider 从二级子菜单改为同层折叠，本次又通过消息输入器“+ → Skills”截图明确要求把该模式记录为样式规范并应用到其他移动端二级菜单。
- 原因：移动端横向空间不足，侧向子弹层容易超出视口、遮挡内容并破坏触控路径；同层折叠可以保留上下文并统一层级交互。
- 已提升至：`SKILL.md`。

### WEB-079

- 状态：`active`
- 范围：移动端项目看板间距
- 规则：项目看板在移动端的外层左右间距统一使用 `px-4`；桌面端可以保留适配侧栏和内容布局的非对称间距，但必须限定在对应响应式断点，不能泄漏到移动端。
- 依据：用户通过移动端看板截图指出左右留白不一致，明确要求两侧都使用 4 级间距。
- 原因：移动端可用宽度有限，对称的 4 级间距能与页面标题和其他主内容保持统一对齐，并避免看板横向滚动区出现无意义的大块左侧留白。

### WEB-080

- 状态：`active`
- 范围：移动端设置弹窗
- 规则：设置弹窗在移动端使用 HeroUI `cover` 圆角弹层，不使用直角全屏形态；正文采用单列布局，分类导航使用 HeroUI `Select` 明确展示当前分类和全部可切换入口，当前分类的图标与文字保持单行。分类选择器与正文之间的正文容器顶部内边距使用 8px；仅在“通用设置”的移动端移除首个设置行自身叠加的顶部内边距，桌面端和其他设置分类保持原间距。设置正文独立纵向滚动，设置项的说明与控件、面板标题与操作在窄屏纵向排列，行级 `Select` 铺满可用宽度。桌面端继续使用左侧分类导航与右侧内容的双栏布局及紧凑控件宽度。
- 依据：用户先通过移动端截图指出桌面双栏设置布局把正文挤成逐字换行，随后明确指出横向分类列表只露出当前项且无法切换其他分类，并进一步要求分类值单行、表单选择器使用 100% 宽度；之后明确要求移动端设置不要使用直角全屏形态，并通过截图要求只收紧分类选择器与正文之间的顶部间距；随后进一步指出“通用设置”首个设置行的顶部留白仍需仅在该页移动端收紧。
- 原因：移动端横向空间不足以同时容纳固定宽度导航或完整分类列表；单列选择器能明确表达切换操作，`cover` 弹层则保持设置界面与其他 HeroUI 工作流一致的圆角和层级。

### WEB-081

- 状态：`active`
- 范围：移动端侧边栏导航
- 规则：从移动端侧边栏发起的程序化导航，在切换主内容前必须同时收起侧边栏；设置中“创建技能”进入新会话时同样适用。
- 依据：用户通过移动端截图明确指出，点击“创建技能”后侧边栏仍保持展开。
- 原因：移动端侧边栏是覆盖主内容的临时导航层；导航完成后保持展开会遮挡新页面，也与侧边栏菜单项的默认收起行为不一致。

## 维护规则

- 新规则使用下一个数字 ID。
- 反馈只是细化范围或措辞时，直接更新已有条目。
- 新反馈替代旧规则时，把旧条目标记为 `superseded` 并添加“替代规则：WEB-XXX”，不要静默删除冲突历史。
- 规则已经进入 `SKILL.md` 或 `project-map.md` 时，标记为 `promoted`。
- 只记录足以改变未来前端实现选择的具体规则。
