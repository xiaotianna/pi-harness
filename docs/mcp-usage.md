# MCP 连接与使用

当前已提供 MCP 工具的基础闭环；完整任务进度见 [实施台账](./mcp-client-host-plan.md)。

## 在哪里连接

1. 登录 PI Harness，打开侧栏的设置，选择 **MCP 服务器**。
2. 点击 **添加服务器**，通过 Tab 选择 **表单配置** 或 **JSON 配置**。表单支持 Streamable HTTP、本地命令（stdio）和旧版 SSE；JSON 使用常见的 `mcp.json` 顶层 `mcpServers` 结构，并可一次添加多个服务器。
3. JSON 示例：

   ```json
   {
     "mcpServers": {
       "time": {
         "type": "streamable_http",
         "url": "https://example.com/mcp"
       }
     }
   }
   ```

4. HTTP 填写完整 MCP endpoint。本机/局域网地址需要在表单中明确打开对应选项；查询参数、URL 内嵌账号密码目前不支持。本地命令使用 `command` 和可选的字符串 `args` 数组，不通过 Shell 执行。
5. 保存配置。如果服务器需要认证，HTTP 先开启「使用请求头凭据」，再点击「设置凭据」录入请求头 JSON；stdio 凭据使用环境变量 JSON。JSON Tab 不接收 `headers` 或 `env`，避免凭据混入普通配置。凭据只写入 daemon，不回显到页面或放入浏览器持久存储。
6. 点击 **连接服务器**，配置由所有项目共用，无需选择或绑定项目。审阅服务器地址或完整命令，点击 **信任并测试连接**。
7. 测试通过后可展开工具数量行查看目录。在任意项目发起下一轮对话，Agent 即可选择 MCP 工具；调用前通过现有审批卡请求批准。

JSON 是用户侧导入/导出的标准形态；daemon 会在 API 边界补齐安全默认值并把非敏感运行配置标准化保存到 SQLite，不会在工作区写入可被 Agent 修改的 `mcp.json` 文件。

设置内容来自当前运行的 Web 和 daemon。如果更新源码后仍找不到入口或出现响应格式错误，请确认两个进程都已加载最新代码。本次实现未自动重启或启动开发服务。

## 授权与状态

- 保存配置不会启动服务器。连接信任与工具调用批准是两个独立动作。
- 服务器配置、凭据、信任和启用状态均为全局设置。「停用服务器」与「撤销信任」会使所有会话中的该服务器连接失效。
- 本地命令当前只支持 POSIX trusted 模式，以当前账户权限运行，未提供操作系统沙箱。固定工作目录不能限制进程只能访问工作区。isolated 模式及 Windows 会明确拒绝。
- 三种本地审批策略，包括「完全访问」，都不会自动批准外部 MCP 工具。Plan 尚未确认时也不能调用 MCP 工具。
- 每轮运行开始时固定工具清单，新增连接在下一轮生效；撤销、停用或修改配置会即时使旧调用失效。审批过程中定义变化时，本次调用会被阻止。
- 「测试通过」表示最近一次目录测试成功。测试不依赖项目，使用独立临时目录，结束即回收连接与目录。会话按需建立各自的 Client，使用该会话固定的工作目录；空闲连接会被回收。
- 取消连接测试不会撤销已经保存的配置或授权；需要停止后续使用时，选择停用或撤销信任。

## 错误与限制

| 情况 | 处理方式 |
|---|---|
| 配置版本冲突 | 刷新状态后重试，避免覆盖其他窗口的修改 |
| 需要凭据 | 设置该服务器的请求头或环境变量凭据，再重新信任 |
| 连接失败 | 检查命令是否存在、HTTP endpoint 是否正确，以及私网访问选项 |
| 运行无法准备 MCP 工具 | 检查工具 schema 是否兼容、定义大小，以及全局启用的服务器数量 |
| 工具定义变化 | 结束当前轮后重新发起一轮，取得新的工具快照 |
| 执行结果未知 | 连接中断、超时或返回校验失败不代表操作没有发生；先核实外部数据，再决定是否发起新调用 |

当前每轮最多加载 64 个 MCP 工具，工具定义合计不超过 512 KiB；复杂递归、外部 schema 引用和超出安全范围的正则会拒绝。暂未提供工具级选择器。

工具文本与结构化结果有界输出，并移除已知连接凭据的原值；图片、音频和二进制资源当前显示未展开提示，资源链接仅作为引用返回，不自动读取。目录中的资源和提示模板目前仅显示发现数量。OAuth 浏览器授权、资源/提示模板交互、Completion、Elicitation、Sampling、OS 沙箱及完整崩溃恢复仍在实施中。

已有配置通过 015 migration 自动升级：只移除旧的项目绑定表，保留服务器、启用状态、信任与凭据。已有启用且受信任的服务器升级后供全部项目使用。

本阶段已验证临时数据库升级、模拟 MCP 服务及实际 Agent Loop；已在登录后的浏览器检查全局入口、添加弹窗、连接方式切换、开关键盘操作与必填校验。真实第三方服务和完整生产矩阵尚未验收。
